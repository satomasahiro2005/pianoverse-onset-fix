# binary sample containers / raw audio — never commit licensed sample data
*.pak
*.wav
# raw PCM excerpt of the sample (regenerate locally; kept out of the public repo)
waveform_a3.csv

# python
__pycache__/
*.pyc

# scratch
*.tmp
test_out.*
