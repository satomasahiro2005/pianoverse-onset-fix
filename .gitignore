# binary sample containers / raw audio — never commit licensed sample data
*.pak
*.wav
# raw PCM excerpt of the sample (regenerate locally; kept out of the public repo)
waveform_a3.csv

# python
__pycache__/
*.pyc

# scratch
*.tmp
test_out.*

# your own measurement runs (walkthrough output) / local backups
my_*.csv
*.old.csv

# data/ is your workspace: one folder per model/mic. Only the bundled YF3
# reference set (data/yf3/) and the README are tracked; your own runs are not.
/data/*
!/data/yf3/
!/data/README.md
